import styled from 'styled-components';
import { color, space, layout, typography, flexbox, border, shadow } from 'styled-system';

export const Box = styled.div`
  ${color}
  ${space}
  ${layout}
  ${flexbox}
  ${border}
  ${shadow}
  ${typography}
`;
